export interface casaCreacionDTO {
    nombre: string;
    descripcion: string;
    foto: File;
}

export interface casaDTO {
    id: number;
    nombre: string;
    descripcion: string;
    foto: string;
}